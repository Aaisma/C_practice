#include<stdio.h>

int main()
{
    int age;
    float weight;

    scanf("%d%f", &age, &weight); //input from user and storing it to memory

    printf("age = %d \nweight = %f\n", age,weight);

    return 0;
}
